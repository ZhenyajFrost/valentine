name: valentine for my girl

on: 
 push: 
   branches: {"master"}
  workflow_dispatch:

jobs:
  build: 
    steps: 
      - uses: action/checkout@v3

      - name: Install dependencies
        run: npm ci

      - name: Build App
        run: npm run build

      - name: Deploy 🚀
        uses: JamesIves/github-pages-deploy-action@v4
        with:
          folder: dist
